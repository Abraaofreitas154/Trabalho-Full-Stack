{
  "name": "frontend",
  "private": true,
  "version": "1.0.0",
  "description": "Frontend PWA em HTML/CSS/JS vanilla para gerenciamento de produtos e clientes",
  "scripts": {
    "dev": "npx live-server --port=3000 --host=localhost",
    "build": "echo 'Nenhum build necessário - arquivos estáticos prontos'",
    "preview": "npx live-server --port=3000 --host=localhost"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}

